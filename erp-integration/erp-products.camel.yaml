- rest:
    id: rest-ab0e
    description: ERP Products Service
    path: products
- route:
    id: erp-products-route
    from:
      id: from-d4e1
      uri: direct
      parameters:
        name: erp-products-route
      steps:
        - removeHeaders:
            id: removeHeaders-274e
            pattern: CamelHttp*
        - to:
            id: to-0dda
            uri: http
            parameters:
              httpUri: host.docker.internal:4004/odata/v4/simple-erp/Products
              httpMethod: GET
              authPassword: "{{env:ERP_PASSWORD}}"
              authMethod: Basic
              bridgeEndpoint: false
              authUsername: "{{env:ERP_USER}}"
        - unmarshal:
            id: unmarshal-8527
            json:
              id: json-afba
        - setBody:
            id: setBody-26b9
            expression:
              simple:
                id: simple-3c05
                expression: ${body[value]}
        - marshal:
            id: marshal-2c19
            json:
              id: json-0255
